# Public API origin (no trailing /api). Used by the Vite dev server and production builds.
VITE_API_URL=http://localhost:4000

# Optional: ngrok tunnel for mobile OAuth testing (see backend/.env.example)
# NGROK_HOST=your-subdomain.ngrok-free.dev
# VITE_USE_NGROK_TUNNEL=1
# VITE_NGROK_HMR=1
