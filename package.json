{
  "name": "engagehub",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "client": "cd frontend && npm run dev",
    "server": "cd backend && npm install && npm start",
    "server:dev": "cd backend && npm run dev",
    "dev": "concurrently -k -n server,client -c blue,green \"npm run server:dev\" \"npm run client\"",
    "install:all": "npm install && cd frontend && npm install && cd ../backend && npm install",
    "build": "npm --prefix frontend run build",
    "vercel-build": "npm --prefix frontend install && npm --prefix frontend run build",
    "tunnel:cf": "cloudflared tunnel --url http://localhost:5173"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
